{
  "name": "web",
  "private": true,
  "version": "1.0.0",
  "description": "SlyxUp Admin — control panel + docs for the SlyxUp Stack (React 19 + Vite + Tailwind v4)",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 4173",
    "lint": "oxlint",
    "typecheck": "tsc -b --noEmit",
    "deploy": "pnpm build && wrangler pages deploy dist --project-name=stack-slyxup-online --branch main --commit-dirty=true",
    "deploy:preview": "pnpm build && wrangler pages deploy dist --project-name=stack-slyxup-online --branch preview --commit-dirty=true",
    "cf:typegen": "wrangler types"
  },
  "dependencies": {
    "@slyxup/ui": "workspace:*",
    "clsx": "^2.1.1",
    "lucide-react": "^1.39.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.3",
    "tailwind-merge": "^3.6.0",
    "zustand": "^5.0.15"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.1.0",
    "oxlint": "^1.79.0",
    "tailwindcss": "^4.3.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
